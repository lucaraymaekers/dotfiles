#!/bin/sh

set -eu

if [ -f ".tm_nnd" ]
then
	. ./.tm_nnd
else
	Name="$1"
	shift
	Command="$*"
fi

tmux split-window -b -l 83% -h "nnd -s --session="$Name" --tty=$(tty) $Command"

clear
sleep 1000000
