#!/bin/sh

tmp="/tmp/mpdf"
choice="$(find ${1:-$HOME/docs $HOME/dl} -iname "*.pdf" 2> /dev/null | sort |
	tee "$tmp" |
	sed "s|^$HOME|\~| ; s|\([^/]\)[^/]*/|\1/|g" |
	commander -x -n)"
file="$(sed -n "${choice}p" "$tmp")"
[ -r "$file" ] || exit 1
zathura "$file" &
