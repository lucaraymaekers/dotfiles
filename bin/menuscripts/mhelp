#!/usr/bin/env sh

program="$(commander -rcd)"

[ "$program" ] || exit 1

option="$("$program" --help |
	# Parse options
	grep -- "^ *-[-a-zA-Z0-9]* " |
	tr -s ' ' |
	sort | uniq |
	column -l 2 -t |
	commander -lxcd |
	awk '{print $1}')"

[ "$option" ] || exit 1

setsid "$program" "$option"
