#!/bin/sh
pics="$(xdg-user-dir PICTURES)"
mkdir -p "${pics:=$HOME/pics}/favorites"
wal="$(realpath "$pics/wallpaper")"
diff "$wal" "$pics/favorites/$(realpath "$wal" | xargs basename)" &&
	exit
cp "$wal" "$pics/favorites"
echo "$wal"
